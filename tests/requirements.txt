# Pinned exact versions (generated from a working venv via `pip freeze`).
# To refresh: bump versions in a venv, run the test suite green, then `pip freeze`.
pytest==9.0.3
pytest-asyncio==1.3.0
requests==2.33.1
grpcio==1.80.0
grpcio-tools==1.80.0
Pillow==12.2.0
numpy==2.4.4
aiohttp==3.14.0
protobuf==6.33.6
